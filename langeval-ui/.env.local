# API Configuration
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Backend Services (Internal via Server Components or API Routes)
RESOURCE_SERVICE_URL=http://localhost:8003
ORCHESTRATOR_SERVICE_URL=http://localhost:8001
IDENTITY_SERVICE_URL=http://localhost:8002
GEN_AI_SERVICE_URL=http://localhost:8006

# Note: Since we are running in Docker, if Next.js runs locally (on host), 
# "localhost" works. If Next.js runs in Docker, use service names.
# Assuming Next.js runs on HOST (npm run dev).
